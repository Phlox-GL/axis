# Axis migration notes

- Migrated the project to Calcit 0.13.29 and the merged Phlox main branch.
- Replaced legacy nullable field access with `get` plus `option:unwrap-or`.
- Updated Phlox cursor operations to tuple dispatch and modernized the Vite workflow.
- Verified `calcit --check-only`, JS generation, and the Vite production build.
